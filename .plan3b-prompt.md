Выполни имплементационный план `docs/superpowers/plans/2026-06-12-platform-admin-plan3b.md` в этом репозитории (/root/projects/meet-mt, ветка multi-tenant-core-phase1) по скиллу superpowers:subagent-driven-development: на каждую задачу — свежий субагент-имплементер, затем ревью соответствия плану и ревью качества, фикс-циклы ≤3; коммит после каждой задачи (сообщения на русском, стиль — как в git log); чекбоксы в плане проставляй по мере выполнения.

Перед стартом прочитай спеку `docs/superpowers/specs/2026-06-12-platform-admin-and-team-dashboards-design.md` (секции 1–5, 8–10). Субагентам передавай полный текст их задачи из плана + ссылку на спеку.

Модельная политика субагентов (Agent tool, параметр model):
- имплементеры и ревьюеры — `sonnet`;
- задачи 3 (контур-гейт), 12 (impersonation), 14 (guards самовыпила), 16 (manager-scoping) — имплементер и оба ревьюера `opus`;
- финальное сквозное ревью всей ветки в самом конце — `fable`.
- `haiku` не использовать вообще.

Жёсткие ограничения (нарушение = провал, остановись и запиши проблему вместо импровизации):
- Работать ТОЛЬКО в /root/projects/meet-mt. НЕ трогать /root/projects/meet, /root/projects/realestate, /root/projects/silentqa, /root/projects/dental*, /root/projects/fillers* — живые прод-чекауты.
- Никаких systemctl, git push, деплоя, правок /etc/* и Caddyfile. Деплой делает владелец отдельно по ранбуку.
- БД: создавать/дропать можно ТОЛЬКО plan3b_scratch (sudo -u postgres). Живые БД meet/realestate/silentqa/dental/fillers не трогать. Redis не флашить, ключи живых нэймспейсов не удалять.
- AmoCRM-токены не трогать, никаких curl к amocrm.

Тесты:
- backend: `cd /root/projects/meet-mt/backend && ../.venv/bin/python -m pytest tests/ -q` (база перед стартом: 81 passed);
- worker (регресс-чек в конце): `cd /root/projects/meet-mt/worker && set -a; source ../.env; set +a; ../.venv/bin/python -m pytest -q` (база: 149 passed);
- TestClient БЕЗ context manager (lifespan гонит реальные миграции); фикстура fake_redis; asyncio.run для сидинга.

По завершении всех 19 задач:
1. Полные прогоны обеих суит.
2. Финальное сквозное fable-ревью диапазона коммитов Plan 3b (от коммита после 5ff1997 до HEAD); найденное чинить отдельными коммитами (имплементеры sonnet/opus по важности).
3. Записать отчёт в /root/projects/meet-mt/.plan3b-result.md: статус по задачам, итоговые числа тестов, что нашло финальное ревью, список коммитов, отклонения от плана (если были и почему).
НЕ деплоить. НЕ мержить ветку. НЕ пушить.
